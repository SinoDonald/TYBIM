<Window x:Class="TYBIM_2025.CreateRoomWall.RoomSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="選擇房間與牆類型" Height="450" Width="600" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
                <TreeView ItemsSource="{Binding Levels}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Rooms}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" VerticalAlignment="Center" Margin="2"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <StackPanel>
                    <TextBlock Text="請選擇要應用的牆類型：" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ComboBox ItemsSource="{Binding WallTypes}" 
                              SelectedItem="{Binding SelectedWallType}" 
                              DisplayMemberPath="Name" 
                              Margin="0,0,0,20"/>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="確定" Height="30" Width="100" Margin="0,0,10,0" Click="BtnOK_Click"/>
            <Button Content="取消" Height="30" Width="100" Click="BtnCancel_Click"/>
        </StackPanel>

    </Grid>
</Window>